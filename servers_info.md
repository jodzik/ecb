# servers info

Реализации серверов(директории `*-server`) должны содержать минимальный набор endpoints:
`DEVICE_INFO(65281)`, `PICK(65282)`. См. стандартные идентификаторы данных в [README.md](./README.md).
